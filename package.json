{
  "name": "flatbush-transportation",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 execute flatbush-transportation --remote --file=worker/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}